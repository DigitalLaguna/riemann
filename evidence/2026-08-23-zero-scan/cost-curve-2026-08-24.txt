# zero-scan-1e5 cost structure — anomaly resolution, tick 193 (2026-08-24 15:08-15:20Z)
#
# ANOMALY: handoff (tick 187) recorded rate 0.262 s/step at i=230000 and ETA
# ~08-26 21:00Z. Measured from full.stderr (append-only): the 225000->230000
# block took 60296-57286 = 3010 s = 0.602 s/step. The 0.262 figure matched the
# 105000->110000 block (1294 s / 5000 = 0.259 s/step) — a stale number from
# ~14 h earlier. Rate history (s/step per 5000-step block, from full.stderr):
#   t~1e3-1e4: 0.050-0.084 | t~2.5e3-1e4: 0.106-0.201 (plateau)
#   jump at t~1.07e4 (block 105000-110000): 0.259
#   plateau t~1.1e4-2.2e4: 0.326-0.351
#   jump at t~2.25e4 (block 220000-225000): 0.540
#   plateau t~2.25e4-2.5e4: 0.600-0.607  <- current
#
# ROOT CAUSE (machine-verified, mpmath 1.3.0, /home/niklas/.local/lib/python3.12/
# site-packages/mpmath/functions/zeta.py):
#   line 554: `if abs(im) > 500*prec and 10*re < prec and derivative <= 4 or \
#                method == 'riemann-siegel':`  -> rs_zeta (Riemann-Siegel)
#   line 666: `M2 = prec // 3`  (EM truncated-sum start)
#   line 719: `M1, M2 = M2, M2*2`  (doubling loop until Bernoulli tail < tol)
#   For s = 1/2+it, re(s)=1/2 > 0, so N (Bernoulli terms) is fixed and M2
#   doubles until the last correction term drops below 2^-prec. Cost per
#   evaluation ~ M2_final, a STEP FUNCTION of t (jumps ~2x at thresholds).
#   mp.mp.dps=30 -> prec = 103 bits (measured) -> RS branch at t > 500*103 = 51500.
#
# TIMING (this tick, 5-iteration mean, 30 dps, Z(t) = re(e^{i theta_asym} zeta(1/2+it))
# with the scan's exact theta_asym):
# t=  10000     31.501 ms/eval
# t=  11000     50.051 ms/eval
# t=  20000     50.601 ms/eval
# t=  23000     86.991 ms/eval
# t=  40000     87.168 ms/eval
# t=  46000    155.816 ms/eval
# t=  49000    156.101 ms/eval
# t=  51000    156.015 ms/eval
# t=  60000     43.221 ms/eval
# t=  80000     43.343 ms/eval
# t=  91000     43.340 ms/eval
# t= 100000     43.197 ms/eval
# => EM stepwise jumps at t~1.07e4, ~2.25e4, ~4.5e4 (measured between the
#    bracketing points); RS switch between 51000 and 60000 (consistent with
#    500*prec = 51500).
#
# SCAN STEP COST: each step is 1 Z eval + bisection refinement (zero_scan.py:
# up to 40 iters, break at b-a<1e-10 -> ~30 Z evals per detected zero). Zero
# density rho(t) = (1/2pi) log(t/2pi) ~ 1.3-1.5 per unit t here, so ~0.13-0.15
# zeros/step -> step cost ~ (1 + 30*0.14) * cost_Z(t) ~ 5.2 * cost_Z(t).
# (Model gives 431 ms/step at t=25000 vs measured 607 ms/step; 1.4x gap
# unexplained at this precision — carried as uncertainty in the ETA, not as
# an open anomaly, since the ETA uses measured rates where available.)
#
# CORRECTED ETA (scan at i=250000, t=25001, elapsed 72384 s, 2026-08-24 15:08Z):
#   [25001,45000): 199990 steps x 0.607 s (measured)      = 121394 s
#   [45000,51500):  65000 steps x ~1.1 s (87->156 ms jump) =  71500 s
#   [51500,1e5]:   485000 steps x ~0.30 s (RS, 43 ms)     = 145500 s
#   total ~ 338394 s ~ 94 h -> ETA ~ 2026-08-28 13:00Z (+/- 1 day)
# The RS speedup at t>51500 saves ~1 day vs a pure-EM extrapolation.
# Handoff's 08-26 21:00Z was based on the stale 0.262 s/step rate.
